{
  "name": "shift-floor-planner",
  "private": true,
  "version": "1.0.0",
  "description": "Warehouse shift floor planner with packer CSV analytics",
  "scripts": {
    "test": "node tests/packer-analytics.test.js && node tests/ops-smoke.test.js && node tests/packer-shift-report.test.js && python3 -m unittest tests.test_packer_shift_report",
    "report:packer": "python3 -m packer_shift_report",
    "start": "python3 -m http.server 8080",
    "verify:csv": "node tests/verify-fixtures.js"
  }
}
